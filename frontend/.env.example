# In dev, leave VITE_API_URL empty — Vite proxies /api to localhost:8000.
# In production (Railway), set this to the deployed backend URL, e.g.:
# VITE_API_URL=https://teamtask-api.up.railway.app
VITE_API_URL=
